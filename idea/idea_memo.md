・エラー管理（Scope化）しないとだめ。
・GuiはOutlookにあわせる
・緊急系に色を付ける（Apps）
・KPIのチェック自動化
・Appsで表示させるとき、絞り込み対象として「複数行」を指定するとバグる。
・Appsの画面から受付ステータスを変更したら、自動で受付処理を開始するようにしてほしい。
・Appsで受付一覧を表示する場合、最も古いメールを親として、子メールをツリー表示でぶら下げて表示するようにしたい。
　「Messageid」「InternetMessageid」「Concersationid」の３つのIDを取得しているが、ほかに必要な情報はあるか？
・重複排除の処理がミスっている。多分「InternetMessageid」を外したから。
　→とりあえず時間を取り除いた。
・タスク管理はplannerを使うのもありかも。
・Outlookメールからどのぐらいの情報を引き出すのか？
　を設計したい。
・メールからどのような情報をAIで作成させるのか？
　その情報の評価は？
・リストに何を持たせるのか？
・PowerAppsで何を表示させるのか？
・画面移動はスクリーンを入れ替えるようにボタンから関数を呼び出す。



【大野さんの受付チェック機能】
・ＡＩの受付判定
　受付済になったら、受付受信メールを送信する。（本来は、受付準備→メール送信→受付済み）
・受付済みのメールが、人の手で＜受付Ｎｏ＞＋メール件名で受付処理が行われているか確認したい。

定期実行
    │
    ▼
受付情報取得
（受付済=Yes
 ReceptionCheckStatus=Unchecked）
    │
    ▼
送信済みメール検索
    │
    ▼
完全一致判定
    │
 ├─一致
 │    │
 │    ▼
 │ Lists更新（OK）
 │
 └─不一致
      │
      ▼
   AI Builder
      │
      ▼
   OK / Review / NG
      │
      ▼
   Lists更新

| 表示名（日本語）   | 内部名（英語）                     | 型       | 必須 | 用途                             |
| ---------- | --------------------------- | ------- | -- | ------------------------------ |
| 受付チェック状態   | `ReceptionCheckStatus`      | Choice  | ○  | 未確認 / OK / Review / NG / Error |
| 受付チェック日時   | `ReceptionCheckDateTime`    | 日時      | ○  | 最後にチェックした日時                    |
| 受付チェック信頼度  | `ReceptionCheckConfidence`  | 数値      | -  | AI信頼度（0～100）                   |
| 受付メール件名    | `ReceptionMailSubject`      | 複数行テキスト | ○  | AIが一致と判断した送信メール件名              |
| 受付メール送信日時  | `ReceptionMailSentDateTime` | 日時      | -  | 一致した送信メールの送信日時                 |
| 受付メール送信者   | `ReceptionMailSender`       | ユーザー    | -  | 受付メールを送信した担当者                  |
| 受付チェックコメント | `ReceptionCheckComment`     | 複数行テキスト | -  | AI判定理由・エラー内容                   |
| 受付チェック実施方法 | `ReceptionCheckMethod`      | Choice  | -  | AI / Manual                    |


【夜間コールのフラグ】
・条件式で、17：30＋障害の場合で、分岐

【送信者のメールアドレスから、Microsoft Enter IDの情報を読み取って、登録者名を表示したい。】
・ユーザープロファイルの取得（V2）から「表示名」を参照で解決。

【Toに担当者リストのメールが含まれていなければ、受付担当外とする。】
・To判定を追加。
　Toにシステム保守のメールが含まれていない場合は、部門外メール処理を行う。

【標時間を自動化する。】
以下の列を作成した。
| 論理名    | 物理名            | 型  | 必須 | 更新主体      | 説明                                   |
| ------ | -------------- | -- | -- | --------- | ------------------------------------ |
| 依頼期限   | RequestDueDate | 日時 | 任意 | SE        | お客様や依頼元から指定された期限。例：「7/31までに対応してください」 |
| 対応目標期限 | TargetDueDate  | 日時 | 任意 | システム / SE | 社内で管理する目標期限。SLAや優先度から自動算出または手動設定     |

算出した対応目標期限が受付日時以前になる場合は、
受付日時から1時間後を対応目標期限とする。


【月次報告の作成支援をフェーズ１で実施したい。】
・「メール本文」と「SE対応概要」の列を作成
・別途機能にて、月次レポート支援用のフローを作成し、Appsの画面から呼び出して実行させる。

【Power Appsからのメール表示機能】
・ディープリンクの取得　→　対応可能
　https://outlook.office.com/mail/deeplink/read/<id>?ItemID=<id>&exvsurl=1
 ※OutlookのメッセージIDをエンコードすることで、ディープリンクの作成が可能。
 　複数行テキスト＋リッチテキスト設定だったらうまくいった

  ❌ 1行テキスト：255文字制限で保存不可
　❌ ハイパーリンク：OutlookディープリンクがURL妥当性チェックでエラー
　✅ 複数行テキスト（リッチテキスト有効）：保存成功
